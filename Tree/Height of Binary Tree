  int height(struct Node* node){
        // code here 
        if(node==NULL)
        return 0;
        else{
        int a=max ((height(node->left)),(height(node->right)));
            return 1+a;}
    }
